import React from "react";

export default function Header() {
  return (
    <header className="flex-none relative z-50 flex items-center justify-between h-[var(--h-header)] px-page">
      <span>Bowang Lan</span>
    </header>
  );
}
